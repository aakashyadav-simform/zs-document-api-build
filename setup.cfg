[flake8]
max-line-length = 100
extend-ignore = E203, W503
exclude = .venv, .git, __pycache__, docs
per-file-ignores =
    # conftest sets test env vars before importing app modules (deliberate)
    tests/conftest.py: E402
